{
  "name": "time-tracker",
  "version": "1.0.0",
  "description": "Kim's Time Tracker",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "postinstall": "electron-rebuild -f -w better-sqlite3"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4",
    "@playwright/test": "^1.60.0",
    "electron": "^41.0.0",
    "playwright": "^1.60.0"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  }
}
